services:
  - type: web
    name: sweezen-api
    env: python
    pythonVersion: 3.13
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
